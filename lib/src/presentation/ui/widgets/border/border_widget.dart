import 'package:flutter/material.dart';

const smallBorderRadius = BorderRadius.all(Radius.circular(4.0));

const normalBorderRadius = BorderRadius.all(Radius.circular(8.0));

const biggerBorderRadius = BorderRadius.all(Radius.circular(16.0));
